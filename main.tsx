import { StartApp } from "client/app/App"

/**
 * main is the entrypoint of the application. This file should always
 * just include the startup function of the app and call it.
 *
 * The reason for this is so users can follow a path to where the code is
 * from the root directory.
 */
function main() {
	StartApp()
}
main()
